spec:
  replicas: 2
  selector:
    matchLabels:
      app: brain-task
  template:
    metadata:
      labels:
        app: brain-task
    spec:
      imagePullSecrets:
      - name: ecr-secret
      containers:
      - name: your-container
        image: your-image
        ports:
        - containerPort: 80